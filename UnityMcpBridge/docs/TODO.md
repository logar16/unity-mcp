# Task: Improve Actions

The get_scene_hierarchy returns so much info and could use a filter to optionally return only the objects and optionally their children and optionally the transform info.

execute_menu_item works but is there a way to get all the available menu items?
